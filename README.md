# Bash script collection

1. ```generateAospPatchScript.sh``` - a meta-script, a script that is used to generate a  script with the correct commands in it, to patch in bulk many AOSP security patches ( no repo tool, just good old patch )
